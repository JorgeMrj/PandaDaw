meta {
  name: Remove Linea Carrito
  type: http
  seq: 9
}

delete {
  url: {{baseUrl}}/Carrito/lineas/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
  res.body.id: isNumber
  res.body.lineas: isArray
}
