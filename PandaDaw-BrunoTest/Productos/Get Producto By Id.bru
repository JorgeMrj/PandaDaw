meta {
  name: Get Producto By Id
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/Productos/1
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.id: eq 1
  res.body.nombre: isString
  res.body.precio: isNumber
  res.body.stock: isNumber
  res.body.categoria: isString
}
